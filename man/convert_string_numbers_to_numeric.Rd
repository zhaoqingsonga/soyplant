% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/baiaoyun.R
\name{convert_string_numbers_to_numeric}
\alias{convert_string_numbers_to_numeric}
\title{将数据框中仅含数字或空值的列转换为数字类型的函数}
\usage{
convert_string_numbers_to_numeric(input_df)
}
\arguments{
\item{input_df}{输入的数据框，需要对其中符合条件的列进行类型转换的数据框对象，类型应为 `data.frame`。}
}
\value{
返回处理后的新数据框，原数据框中仅含数字或空值的列已被转换为数字类型，其他列保持不变，类型仍为 `data.frame`。
}
\description{
此函数接受一个数据框作为输入，遍历其每一列，对于那些元素仅包含数字、`NA` 值或空字符串的列，
将其转换为数字类型（`numeric`），方便后续进行数值相关的计算与分析。
}
\examples{
# 构造示例数据框，包含不同情况的列
df <- data.frame(
    col1 = c("1", "2", "3"),
    col2 = c("a", "b", "c"),
    col3 = c(NA, "", "4"),
    col4 = c("5", "6x", "7")
)

# 调用函数进行类型转换
result_df <- convert_string_numbers_to_numeric(input_df = df)
print(result_df)

}
