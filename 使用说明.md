# soyplant 包使用说明

**soyplant** 是一个大豆育种数据管理 R 包，用于管理从杂交组合到田间种植的全流程数据。

---

## 1. 安装与加载

```r
# 加载所有源文件
source("R/config.R")        # 定义 soy_traits 等全局数据
source("R/info_id.R")
source("R/combination.R")
source("R/population.R")
source("R/plant.R")
source("R/line.R")
source("R/primary_productivity.R")
source("R/advanced_productivity.R")
source("R/advance_breeding_stage.R")
source("R/planting.R")
source("R/design_plot.r")
source("R/utils.R")
source("R/baiaoyun.R")
source("R/yield.R")
source("R/traits.R")
source("R/sqlite.R")
source("R/deal_trait.R")
source("R/transname.R")
source("R/savewb.R")

# 全局变量 field 和 soy_traits 必须在使用前加载
field <- read.table("data/field.txt", header = TRUE)
```

---

## 2. 育种流程总览

```
杂交组合(mapa) → 群体(population) → 单株(plant) → 株行(line)
  → 初级产比(primary) → 高级产比(advanced) → 多点鉴定 → 田间种植(planting)
```

每个阶段的核心操作：
- **晋级**：将符合条件（`next_stage` 指向下一阶段）的记录筛选出来
- **世代更新**：名称追加世代号（如 `A:F1` → `A:F2`），`f` 字段 +1
- **ID 生成**：每条记录获得全局唯一 `id` 和 `fieldid`
- **溯源**：记录 `source`、`former_fieldid`、`former_stageid`

---

## 3. 核心函数详解

### 3.1 杂交组合

#### `combination(ma, pa, memo)`
配制杂交组合，返回母本 × 父本的全部组合。

```r
# 基本用法：两个母本和三个父本产生6个组合
combi <- combination(ma = c("冀豆12", "冀豆17"),
                     pa = c("中联豆6001", "中联豆6024", "中联豆6033"),
                     memo = "转基因试验")
# 返回: data.frame(ma, pa, mapa, memo)
#       mapa = ma/pa，用连接符连接
```

#### `combine_slash_connector(mother_vec, father_vec, sep = "/")`
合并母本和父本字符串，用重复的连接符连接。

```r
# 连接符重复次数 = 两者中最长连续连接符数量 + 1
combine_slash_connector("冀豆12/冀豆17//冀豆19", "2232/788")
# 返回: "冀豆12/冀豆17//冀豆19///2232/788"
```

#### `combination_from_dataframe(mydata, only = TRUE, order = FALSE)`
从文件数据框配组合，`mydata` 必须包含三列：母本、父本、备注。

```r
# 从外部数据框配制杂交组合
mydata <- data.frame(
  mother = c("冀豆12", "冀豆17"),
  father = c("中联豆6001", "中联豆6024"),
  note = c("试验1", "试验2")
)
combi <- combination_from_dataframe(mydata)
# only = TRUE: 去除重复组合
# order = TRUE: 按组合名称排序
```

#### `get_combination(mydata, prefix = "ZJ", startN = 1, only = TRUE, order = FALSE)`
将组合数据框升级为完整的杂交阶段记录（带 id、fieldid、stageid 等字段）。

```r
mydata <- data.frame(ma = c("冀豆12","冀豆17"), pa = c("中联豆6001"), memo = "试验")
my_combi <- get_combination(mydata, prefix = "ZJ", startN = 1)
# 生成字段: id, user, stageid, name(F0), f=0, stage="杂交", next_stage="群体"
# name格式: ZJ001F0, ZJ002F0, ...
```

#### `combination_matrix(my_combi)`
将组合表转换为母本 × 父本矩阵视图。

```r
# 返回矩阵，行=母本，列=父本，单元格=组合名称
matrix_view <- combination_matrix(my_combi)
```

#### `convert_to_pairs(data_frame_input, feature_cols = NULL, mylag = "1")`
将矩阵格式转换为配对数据框，用于处理遗传分析矩阵。

```r
# data_frame_input: 方阵数据框，必须包含"mapa"单元格在对角线位置
# feature_cols: 数值向量，指定要提取的特征列索引
# mylag: 识别有效配对的标记值，默认为"1"
pairs_data <- convert_to_pairs(data_frame_input)
```

---

### 3.2 群体

#### `get_population(my_combi, start_num = 1)`
从杂交组合中晋级群体。

```r
my_pop <- get_population(my_combi, start_num = 1)
# 自动过滤 next_stage == "群体" 的记录
# 世代号追加规则:
#   - 名称如 "xxxF3" → "xxxF4"
#   - 名称如 "xxx:3" → "xxx:4"
#   - 其他格式 → "xxx:1"
# 新 next_stage = "群体"
```

---

### 3.3 单株

#### `get_plant(my_pop, start_num = 1)`
从群体中升级单株，每个群体按 `sele` 字段值扩展为多行。

```r
# my_pop 必须包含: name, Sele(选择单株数), f, path, fieldid, stageid, process
my_pop <- data.frame(
  name = c("A", "B"),
  Sele = c(2, 3),  # Sele = 选择单株数
  f = c(1, 1),
  path = c("A", "B"),
  fieldid = c("f1", "f2"),
  stageid = c("s1", "s2"),
  process = c("p1", "p2")
)
# 注意: names(my_pop)[2] <- "sele"  # 必须是全小写 "sele"

my_plant <- get_plant(my_pop)
# A (选择2株) → A-1, A-2
# B (选择3株) → B-1, B-2, B-3
# 新 stage = "单株", next_stage = "株行"
```

**注意**：
- `sele` 字段必须为数值型（纯数字），非数字内容会被自动过滤为 NA
- 内部调用 `expand_rows()` 进行行扩展

---

### 3.4 株行

#### `get_line(my_plant, start_num = 1)`
从单株晋级为株行。

```r
my_line <- get_line(my_plant, start_num = 1)
# 过滤 next_stage == "株行" 的记录
# 新 stage = "株行", next_stage = "初级产比"
# 世代 f = f + 1
```

---

### 3.5 初级产比

#### `get_primary(my_line, start_num = 1)`
从株行晋级为初级产比。

```r
my_primary <- get_primary(my_line, start_num = 1)
# 过滤 next_stage == "初级产比" 的记录
# 世代号追加（F1→F2）
# 新 stage = "初级产比", next_stage = "高级产比"
```

---

### 3.6 高级产比

#### `get_advanced(my_primary, start_num = 1)`
从初级产比晋级为高级产比。

```r
my_advanced <- get_advanced(my_primary, start_num = 1)
# 过滤 next_stage == "高级产比" 的记录
# 世代号追加（F2→F3）
# 新 stage = "高级产比", next_stage = "多点鉴定"
```

#### `advance_breeding_stage(candidates, start_num = 1, selection_key, target_key)`
通用晋级函数，适用于任意阶段晋级。

```r
# 从预试晋级到区试
my_district <- advance_breeding_stage(
  candidates,
  selection_key = "预试",
  target_key = "区试"
)

# 参数说明:
# - candidates: 待晋级的数据框
# - selection_key: 当前阶段名称（匹配 next_stage，支持正则和忽略大小写）
# - target_key: 目标阶段名称
# - 自动执行: 过滤、ID生成、世代更新、字段更新、对齐到field模式
```

---

### 3.7 ID 和编号生成

#### `generate_id(start_num = 1, end_num = 10, char = "g", digit_length = 5, include_datetime = TRUE)`
生成带时间戳的全局唯一 ID。

```r
ids <- generate_id(start_num = 1, end_num = 5, char = "g", digit_length = 5)
# 返回格式: g + 年(2位) + 月(base62) + 日(base62) + 时(base62) + 分(base62) + 秒(base62) + 序号
# 例: g26040814301200001

ids <- generate_id(start_num = 1, end_num = 5, char = "g", digit_length = 5, include_datetime = FALSE)
# 不含时间戳: g00001, g00002, ...
```

#### `generate_stageid(start_num = 1, end_num, char = "24GC", digit_length = 4)`
生成田间编号（不含时间戳）。

```r
stageids <- generate_stageid(start_num = 1, end_num = 10, char = "GC", digit_length = 3)
# 返回: GC001, GC002, ..., GC010
```

#### `get_computer_nodename()`
获取电脑节点名称。

```r
nodename <- get_computer_nodename()
# 返回: Sys.info()["nodename"]
```

#### `get_next_id(my_primary)`
从已有 ID 提取下一个 ID 的前缀和序号。

```r
my_primary <- data.frame(ID = c("g26040814301200123", "g26040814301200124"))
result <- get_next_id(my_primary)
# result$id_n: 下一个序号 (125)
# result$id_p: ID前缀 ("g2604081430120")
```

#### `to_base62(num)`
将数字转换为 62 进制（0-9, a-z, A-Z）。

```r
to_base62(10)   # 返回: "a"
to_base62(62)   # 返回: "10"
to_base62(100)  # 返回: "1c"
```

---

### 3.8 田间种植设计

#### `planting(my_primary, ck, interval, s_prefix, rp, treatment, place, ckfixed, digits, rows, restartfid, startN)`
完整种植方案设计，自动完成：插入对照 → 添加重复 → 添加处理 → 添加地点 → 编号。

```r
# 基本用法
planted <- planting(
  my_primary = my_primary,      # 来自 get_primary 的数据
  ck = c("冀豆12", "冀豆17"),  # 对照名称（可多个）
  interval = 3,                # 每隔几个材料插一个对照
  s_prefix = "GC",            # 田间编号前缀
  rp = 2,                      # 重复数（第一重复顺序，其余随机）
  treatment = c(""),           # 处理因素（留空或多个，如 c("高密", "低密")）
  place = c("石家庄", "德州"), # 试验地点
  ckfixed = TRUE,              # TRUE=固定插入对照 FALSE=随机对照
  digits = 3,                  # 田间编号位数
  rows = 6,                    # 每块行数
  restartfid = FALSE,          # 不同地点是否重新编号
  startN = 1
)
```

**田间编号生成规则**：
- 格式：`前缀 + 年份(2位) + 月(base62) + 日(base62) + 时(base62) + 分(base62) + 秒(base62) + 序号`
- 例：`GC260408143012001` = GC + 26年 + 04月 + 08日 + 14时 + 30分 + 12秒 + 001

**对照插入策略**：
- `ckfixed = TRUE`（固定）：等间隔插入对照，每重复内对照位置相同
- `ckfixed = FALSE`（随机）：对照位置每重复随机，且相邻重复并排两列材料不相同

**地点编号策略**：
- `restartfid = TRUE`：每个地点重新从 1 开始编号（依赖时间戳间隔 1.1 秒保证唯一性）
- `restartfid = FALSE`：全局连续编号

---

### 3.9 田间重复设计（内部函数）

#### `addrpckfixed(my_primary, ck, interval, s_prefix, rp, digits, startN)`
固定对照的田间重复设计。

```r
# 第一重复顺序排列，其余重复随机
# 并排两列材料不相同
result <- addrpckfixed(my_primary,
                       ck = "冀豆12",
                       interval = 3,
                       s_prefix = "GC",
                       rp = 2,
                       digits = 3,
                       startN = 1)
```

#### `addrpck(my_primary, ck, interval, s_prefix, rp, digits, startN)`
随机对照的田间重复设计。

```r
# 对照位置每重复随机
# 并排两个重复不相同
result <- addrpck(my_primary,
                  ck = c("冀豆12", "冀豆17"),
                  interval = 3,
                  s_prefix = "GC",
                  rp = 3,
                  digits = 3,
                  startN = 1)
```

---

### 3.10 田间小区设计

#### `expand_rows(df, replicate_col = "rows", sequence_col = "seq_id")`
按某列数值展开数据框行。

```r
df <- data.frame(
  id = c(1, 2, 3),
  name = c("A", "B", "C"),
  rows = c(2, 3, 1)
)
expanded <- expand_rows(df, "rows", "seq_id")
# 返回 6 行: A(×2), B(×3), C(×1)
# 新增 seq_id 列表示每组中的序号
```

#### `getWaterLaneProtected(blockStructure = "w/23/2r/w/2p/w/2p/23/w")`
解析田间沟路保护行结构字符串。

```r
result <- getWaterLaneProtected("w/23/2r/w/2p/w/2p/23/w")
# 返回列表:
#   totalColumns: 总列数
#   waterColumns: 水沟列索引 (-77)
#   laneColumns: 道路列索引 (-111)
#   protectedColumns: 保护行列索引 (-9)

# 字符串格式说明:
#   w = 水沟
#   r = 道路（数字+r 表示多个道路）
#   p = 保护行（数字+p 表示多个保护行）
#   数字 = 普通地块列数
```

#### `designPlot(blocks = 20, y = 10, bridges, distanceBetweenBlocks1 = 1, distanceBetweenBlocks2 = 0.2, protectedColumns = c(3), protectedBlocks = c(1, 3, 4, 6), waterColumns = c(5), laneColumns = c(9), protectedArea = c(56, 65, 6, 8, 34, 38, 2, 5), designFromLeft = TRUE, plantFromLeft = TRUE, subg = 3)`
生成随机区组设计图。

```r
fi <- designPlot(blocks = 15, y = 8, waterColumns = c(4, 8))
# 返回矩阵，包含:
#   - 地块编号
#   - 水沟列 (-77)
#   - 道路列 (-111)
#   - 保护行 (-9)
#   - 植被区域 (-8)
#   - 累积长度
#   - 地块名称
```

#### `plantByGroup(ma, subg = 3, mysign = -6)`
根据子组标记矩阵进行分组种植标记。

```r
ma <- matrix(rep(0, 200), nrow = 10, ncol = 20)
ma[, 3] <- -77  # 水沟列
ma[, 18] <- -77
result <- plantByGroup(ma, subg = 6)
# 将连续 subg 个 0 的位置标记为 mysign (-6)
```

#### `getSta(fi)`
获取地块统计信息。

```r
fi <- designPlot()
stats <- getSta(fi)
# 返回统计表: 类型, 条数, 行数
```

#### `plant(fi = designPlot(), seed = makelist(), returnfi = TRUE)`
在设计图中种植种子。

```r
# returnfi = TRUE: 返回设计图矩阵
# returnfi = FALSE: 返回种子列表（含行列坐标）
```

---

### 3.11 农业参数计算

#### `zhuhangjv(mu_zhushu = 16000, hangju = 0.5)`
计算株距和每米苗数。

```r
result <- zhuhangjv(mu_zhushu = 16000, hangju = 0.5)
# 返回列表:
#   mi_miaoshu: 每米苗数
#   zhujv: 株距（米）
```

#### `midu(亩密度_万株 = NULL, 行距_厘米 = NULL, 株距_厘米 = NULL)`
根据已知参数计算种植密度（任意两个参数计算第三个）。

```r
# 计算亩密度
result <- midu(亩密度_万株 = 30, 行距_厘米 = 20)
# 返回: 亩密度_万株, 行距_厘米, 株距_厘米, 米株数, 亩用种量_万粒

# 计算株距
result <- midu(亩密度_万株 = 25, 株距_厘米 = 18)

# 计算行距
result <- midu(株距_厘米 = 15, 行距_厘米 = 25)
```

---

### 3.12 产量分析

#### `remove_ck_outliers(df)`
用 IQR 法去除对照组中的亩产异常值。

```r
df <- data.frame(
  MuChan = c(10, 12, 15, 8, 25, 30, 35, 100, 20, 18),
  is_ck = c(0, 1, 1, 0, 1, 1, 0, 1, 0, 1)
)
df_clean <- remove_ck_outliers(df)
# 仅对 is_ck == 1 的行进行处理
# 异常值定义: < Q1 - 1.5*IQR 或 > Q3 + 1.5*IQR
```

#### `find_outliers(MuChan, method = "IQR")`
判断数据中的异常值。

```r
MuChan_data <- c(10, 20, 15, 25, 30, 100, 5)

# IQR 方法
outliers_iqr <- find_outliers(MuChan_data, method = "IQR")

# SD 方法（3倍标准差）
outliers_sd <- find_outliers(MuChan_data, method = "SD")
```

#### `calculate_MuChan_increase_with_multiple_methods(df)`
计算增产百分比并排名。

```r
result <- calculate_MuChan_increase_with_multiple_methods(df)
# 新增列:
#   JiaoLinJinDuiZhaoZengChan: 较临近对照增产%
#   JiaoPingJunDuiZhaoZengChan: 较平均对照增产%
#   JiaoLinJinDuiZhaoWeiCi: 较临近对照位次
#   JiaoPingJunDuiZhaoWeiCi: 较平均对照位次
```

#### `summarize_data(updated, soy_traits)`
对大豆数据进行汇总。

```r
# 筛选 rp == 1 的数据
# 对数值型列按 code 分组汇总计算平均值
# 返回汇总后的数据框
result <- summarize_data(updated_data, soy_traits)
```

#### `convert_columns_to_date(df)`
将数据框中符合日期格式的字符列转换为日期类型。

```r
df <- data.frame(
  date_col = c("2020-01-01", "2021-02-02", "2022-03-03"),
  num_col = c(1, 2, 3)
)
df_converted <- convert_columns_to_date(df)
```

---

### 3.13 百奥云数据对接

#### `replace_headers(original_df, alias_df)`
批量替换数据框表头名称。

```r
# alias_df 必须包含两列: 原表头名称, 别名
alias_df <- data.frame(
  original = c("col1", "col2"),
  alias = c("new_col1", "new_col2")
)
result <- replace_headers(original_df, alias_df)
# 原表头在 alias_df 中有对应则替换，否则转换为合法 R 变量名
```

#### `convert_string_numbers_to_numeric(input_df)`
将只含数字或空值的字符列转换为数值型。

```r
df <- data.frame(
  col1 = c("1", "2", "3"),
  col2 = c("a", "b", "c"),
  col3 = c(NA, "", "4")
)
result_df <- convert_string_numbers_to_numeric(df)
# col1 → 数值型; col2 保持字符型; col3 → 数值型
```

#### `update_dataframe_by_fieldid(reference_df, target_df)`
通过 fieldid 将参考数据更新到目标数据框。

```r
# reference_df 必须包含 "唯一编号" 列
# target_df 必须包含 "fieldid" 列
# 按 fieldid 匹配，只更新两表中字段名完全一致的列
updated <- update_dataframe_by_fieldid(reference_df, target_df)
```

#### `update_baiaoyun(df, updf, overwrite = TRUE)`
更新百奥云数据集的性状信息。

```r
# overwrite = TRUE:  覆盖已有数据
# overwrite = FALSE: 只更新 NA 值
# 备注性状 (T080, T093, T101): 始终追加不覆盖
updated_df <- update_baiaoyun(df, updf, overwrite = FALSE)
```

---

### 3.14 SQLite 数据库

#### `write_df_to_sqlite(df, db_path, table_name, overwrite = FALSE, append = FALSE)`
写入数据框到 SQLite 表。

```r
df <- data.frame(id = 1:3, name = c("A", "B", "C"))
write_df_to_sqlite(df, "breed.db", "my_table", overwrite = TRUE)
```

#### `read_df_from_sqlite(db_path, table_name)`
从 SQLite 表读取数据框。

```r
df <- read_df_from_sqlite("breed.db", "my_table")
```

#### `update_table_from_df(db_path, table_name, data_df)`
通过 fieldid 批量更新 SQLite 表。

```r
# data_df 第一列必须为 fieldid
# 返回 NULL 表示成功；返回缺失字段列表表示部分失败
result <- update_table_from_df("breed.db", "planting", data_df)
```

#### `add_fields_to_table(db_path, table_name, new_field_names)`
向 SQLite 表添加新字段（类型 TEXT）。

```r
result <- add_fields_to_table("breed.db", "planting",
                              c("新性状1", "新性状2"))
result$success  # 成功添加的字段
result$failed   # 失败（已存在）的字段
```

---

### 3.15 性状表生成

#### `traitstable(trait_col = 3, validation_number = 100)`
生成带数据验证的 Excel 性状表（openxlsx 格式）。

```r
wb <- traitstable(trait_col = 3, validation_number = 100)
saveWorkbook(wb, "性状表.xlsx", overwrite = TRUE)

# 支持的验证类型:
#   - C (字符型): 下拉列表验证
#   - D (日期型): 日期验证（>= 2020-01-01）
#   - N (数值型): 范围验证
```

---

### 3.16 数据保存与导出

#### `savewb(origin, material, planting, myview, combi_matrix, filename, overwrite = FALSE)`
保存数据到 Excel 工作簿。

```r
savewb(
  origin = origin_data,      # 原始数据
  material = material_data,  # 材料数据
  planting = planting_data,  # 种植数据
  myview = traits_data,      # 性状调查表
  combi_matrix = NULL,       # 组合矩阵（可选）
  filename = "output.xlsx",
  overwrite = FALSE
)
```

---

### 3.17 字段名称转换

#### `transname(code, toenglish = TRUE)`
中英文字段名互转。

```r
# toenglish = TRUE: 英→中
transname(c("fieldid", "id", "user", "stageid", "ma"))
# 返回: c("田间序号", "材料序号", "用户", "阶段名称", "母本")

# toenglish = FALSE: 中→英
transname(c("田间序号", "材料序号"), toenglish = FALSE)
# 返回: c("fieldid", "id")
```

---

### 3.18 数据处理工具

#### `ensure_and_reorder_columns(df, required_columns)`
确保数据框包含指定字段并调整字段顺序。

```r
df <- data.frame(name = c("A", "B"), stageid = c(1, 2))
required_columns <- c("id", "user", "stageid", "name", "ma", "pa", "mapa",
                      "stage", "next_stage", "f", "sele", "process", "path", "source")
df <- ensure_and_reorder_columns(df, required_columns)
# 缺失字段自动补 NA，并调整到指定位置
```

#### `align_to_field_schema(df, table_pattern)`
对齐数据框字段到 field 模式。

```r
# 根据 field 表定义补齐缺失字段并按 field 顺序排列列
result <- align_to_field_schema(df, table_pattern = "combination")
```

#### `insert_ck_rows(df, ck)`
在每个分组中插入对照行（NA 行）。

```r
df_list <- split(mym, patten)
df_list <- insert_ck_rows(df_list, ck = c("冀豆12", "冀豆17"))
df_list <- do.call(rbind, df_list)
```

#### `increment_generation_name(names, f_vals, max_gen = 9)`
增加名称中的世代数字。

```r
increment_generation_name(c("冀豆12:3", "冀豆15"), c(3, 4))
# "冀豆12:4" (替换已有冒号后缀)
# "冀豆15:5" (追加冒号后缀)

# max_gen = 9: 超过9代不追加
increment_generation_name(c("冀豆12:9", "冀豆15"), c(9, 4))
# "冀豆12:9" (不追加，超过上限)
# "冀豆15:5"
```

#### `rows_to_linenumber(number)`
将行数转换为行号字符串。

```r
rows_to_linenumber(c(3, 2, 4))
# 返回: c("1-3", "4-5", "6-9")
```

#### `create_repeated_dataframe(name, rp)`
创建重复名称的数据框。

```r
create_repeated_dataframe("试验材料A", 3)
# 返回:
#   stageid     name line_number
# 1 试验材料A 试验材料A        1-3
# 2 试验材料A 试验材料A        1-3
# 3 试验材料A 试验材料A        1-3
```

#### `replicate_rows(df)`
根据最后一列的值复制数据框行。

```r
df <- data.frame(
  name = c("A", "B", "C"),
  value = c(10, 20, 30),
  times = c(1, 3, 2)
)
replicated_df <- replicate_rows(df)
# 返回: 6 行
```

#### `create_multiple_pairs_from_df(df)`
从单列数据框创建配对数据框。

```r
# 匹配模式: f开头 + 7个字符 + 5位数字
elements_df <- data.frame(elements = c("fabcdefg12345", "bar", "fghijklm67890", "baz"))
pairs_df <- create_multiple_pairs_from_df(elements_df)
# 返回: fieldid, non_fieldid 两列
```

#### `create_class_matrix(df)`
将数据框转换为 class 宽格式矩阵。

```r
# df 必须包含: fieldid, name_code, level_C
# 返回: fieldid 为行，name_code 为列，level_C 为值的宽表
wide_df <- create_class_matrix(df)
```

#### `read_and_process_planting_tables(folder_path)`
读取文件夹中所有 Excel 文件的 planting 表并处理。

```r
# 读取 folder_path 下所有 .xlsx 文件的 planting 表
# 自动补齐缺失字段，删除多余字段，按 field 表定义排序
all_planting <- read_and_process_planting_tables("data/")
```

#### `convert_specified_columns_to_numeric(input_df, columns_to_convert)`
将指定列转换为数值类型。

```r
df <- convert_specified_columns_to_numeric(df, c("col1", "col2", "col3"))
```

#### `getSub(j, vec, subg)`
从向量中提取子组。

```r
vec <- c(0, 0, 0, -77, 0, 0, 0, 0, -77)
getSub(3, vec, 3)  # 返回: c(0, 0, 0)
```

#### `makelist(myd)`
创建种子列表（返回带序号的列表）。

```r
data <- data.frame(num = 1:3, name = c('JD12', 'JD17', '五星1'), rp = c(3, 10, 6))
result <- makelist(data)
# 返回: result(名称), number(组内序号)
```

#### `makelist2(myd)`
创建种子列表（返回纯重复列表）。

```r
data <- data.frame(num = 1:3, name = c('JD12', 'JD17', '五星1'), rp = c(3, 10, 6))
result <- makelist2(data)
# 返回: 重复的名称向量
```

#### `selectedCol(fi)`
选择设计图中的特殊列（水沟、道路、保护行等）。

```r
fi <- designPlot()
selected <- selectedCol(fi)
```

#### `get_clipboard()`
从剪贴板读取数据。

```r
df <- get_clipboard()
# 相当于: read.table("clipboard", header = TRUE)
```

#### `merge_data_frames_by_row(...)`
左连接多个数据框。

```r
# 按 "id" 列左连接所有数据框
combined <- merge_data_frames_by_row(df1, df2, df3)
```

---

### 3.19 数据下载

#### `download_model(file_name = "soyplant_model.xlsx", dir_path = getwd())`
下载模型数据并导出为 Excel 文件。

```r
download_model(file_name = "soyplant_model.xlsx", dir_path = "output/")
# 依次提取: mapa, combination, population, plant, line, primary, advanced, planting
# 生成 8 个 sheet 的 Excel 文件
```

---

## 4. 全局字段对齐

所有晋级函数末尾调用 `align_to_field_schema(df, table_pattern)`，确保：
1. 数据框包含该阶段所需的全部字段（缺失则补 NA）
2. 字段顺序与 `field` 表定义一致

必需字段顺序示例（combination 阶段）：
```
id, user, stageid, name, ma, pa, mapa, memo, stage, next_stage,
f, Sele, process, path, source, fieldid, former_fieldid, former_stageid
```

如需在数据框中强制对齐字段：

```r
field <<- read.table("data/field.txt", header = TRUE)  # 必须先加载全局 field
df <- ensure_and_reorder_columns(df, required_columns)
```

---

## 5. 完整工作流示例

```r
# 1. 加载
source("R/config.R")        # 定义 soy_traits
source("R/info_id.R")       # ID/编号生成
source("R/combination.R")
source("R/population.R")
source("R/plant.R")
source("R/line.R")
source("R/primary_productivity.R")
source("R/advanced_productivity.R")
source("R/planting.R")
source("R/design_plot.r")
source("R/utils.R")

field <- read.table("data/field.txt", header = TRUE)

# 2. 配制杂交组合
mydata <- data.frame(
  ma = c("冀豆12","冀豆17"),
  pa = c("中联豆6001"),
  memo = "试验"
)
combi <- combination_from_dataframe(mydata)
my_combi <- get_combination(mydata, prefix = "ZJ", startN = 1)

# 3. 晋级群体
my_pop <- get_population(my_combi)

# 4. 升级单株（假设已选择，添加 Sele 字段）
my_pop$sele <- c(2, 3)
names(my_pop)[2] <- "sele"
my_plant <- get_plant(my_pop)

# 5. 晋级株行
my_line <- get_line(my_plant)

# 6. 晋级初级产比
my_primary <- get_primary(my_line)

# 7. 田间种植设计
planted <- planting(my_primary,
                    ck = c("冀豆12"),
                    interval = 3,
                    rp = 2,
                    place = c("石家庄"))

# 8. 导出到 Excel
library(openxlsx)
wb <- createWorkbook()
addWorksheet(wb, "种植")
writeData(wb, "种植", planted)
saveWorkbook(wb, "种植设计.xlsx", overwrite = TRUE)
```

---

## 6. 文件结构

```
soyplant/
├── R/
│   ├── info_id.R              # ID/编号生成
│   ├── combination.R          # 杂交组合
│   ├── population.R           # 群体晋级
│   ├── plant.R                # 单株升级
│   ├── line.R                 # 株行晋级
│   ├── primary_productivity.R # 初级产比晋级
│   ├── advanced_productivity.R# 高级产比晋级
│   ├── advance_breeding_stage.R# 通用晋级
│   ├── planting.R             # 田间种植设计
│   ├── design_plot.r          # 田间小区设计
│   ├── yield.R                # 产量分析
│   ├── baiaoyun.R             # 百奥云数据对接
│   ├── traits.R               # 性状表生成
│   ├── sqlite.R               # SQLite 数据库
│   ├── utils.R                # 通用工具
│   ├── deal_trait.R           # 性状数据处理
│   ├── transname.R            # 字段名转换
│   ├── savewb.R               # 数据保存
│   ├── experiment_design.R    # 试验设计入口
│   ├── field.R                # 农业参数计算
│   ├── abandon.R               # 废弃函数
│   └── config.R               # 全局配置
├── data/
│   ├── field.txt             # 全局字段定义
│   ├── baiaoyun_traits.txt    # 百奥云性状定义
│   ├── mapa.txt              # 组合模板
│   └── qr_trait.txt          # 性状二维码数据
├── man/                       # 函数的 .Rd 帮助文档
└── DESCRIPTION               # 包描述
```

---

## 7. 注意事项

1. **全局变量 `field`**：大部分函数依赖 `field` 数据框定义字段结构，使用前必须加载 `field <- read.table("data/field.txt", header = TRUE)`。

2. **`sele` 字段**：`get_plant` 中的 `sele` 必须为纯数字，非数字内容（字母、混合字符串）会被过滤为 NA。

3. **ID 唯一性**：`generate_id` 依赖时间戳，同一秒内生成的 ID 严格递增，不需要 `Sys.sleep()`。

4. **代数上限**：`increment_generation_name` 默认最大代数 9，超过不追加。

5. **SQLite 并发**：同一数据库连接不可同时读写，建议函数级连接（`on.exit` 保证关闭）。

6. **`sele` vs `Sele`**：晋级函数内部使用 `sele`（全小写），从 Excel 读取时可能是 `Sele`，需注意转换字段名。

7. **百奥云更新**：备注性状（T080, T093, T101）始终追加不覆盖，其他性状按 `overwrite` 参数决定。

8. **时间戳间隔**：`restartfid = TRUE` 时依赖时间戳间隔 1.1 秒保证 ID 唯一性，高频操作需注意。
